# Session Retro: 2026-04-19 21:09

## Problems

**Problem**: Mechanically escalated backlog 017 from Simple to Complex tier because research.md had 3 `## Open Questions` bullets (count ≥ 2 threshold). **Consequence**: An XS doc ticket (three tiny prose edits, explicit AC in the ticket body) ran through two full `/critical-review` passes (spec + plan) with parallel reviewer agents + Opus synthesis. The reviews surfaced genuine issues, but the machinery was disproportionate to a 6-line-of-prose change — the escalation rule treated spec-phase wording questions as complexity signals when they were just deferred decisions the user could answer in 30 seconds.

**Problem**: First-pass spec encoded the `evt:status.bt` semantic flip in direction-ambiguous prose (`"reflects X rather than Y"`) with grep-only acceptance criteria. **Consequence**: An implementer could have written the bullet with the eras inverted and passed every acceptance check — re-planting the exact trap the ticket was meant to prevent. Only the critical-review pass caught it, forcing explicit pre/post-era markers tied to specific symbols in the Requirements section.

**Problem**: First-pass plan had 9 distinct structural defects: ambiguous `$BASELINE_REF` capture timing, no worktree isolation despite `Depends on: none` parallel dispatch on a shared tree, Task 4 "+/commit" title with raw `git log`/`git commit` in Verification (violating the user's global "Always invoke /commit skill" rule), unpaired pre/post-era greps, bare `CLAUDE.md` cross-reference check that accepts title drift, `"1.8.0+"` ban enforced in only 1 of 3 files, no new-bullet format grep, no re-verify step for line-number drift from sibling ticket 014 (which edits the same `server.py`), no content-addressed fallback for Task 3 docstring. **Consequence**: A literal-minded overnight agent executing the first-pass plan could have produced a "passing" commit that silently violated commit-skill convention, skipped the real scope-diff (via `HEAD..HEAD` empty diff), and shipped title drift between the three documentation surfaces.

**Problem**: Failed to anticipate that proposing plan approval to the user would be rejected — proceeded straight to the approval question without first asking whether the critical-review-revised plan was worth continuing given the ticket's XS size. **Consequence**: User interrupted the approval question and invoked /fresh, ending the session before Implement could run. The plan is approved-pending; a fresh window must decide whether to continue the lifecycle, descope, or abandon the critical-review machinery for this ticket.
