# Session Retro: 2026-04-21 21:27

## Problems

**Problem**: Initial response to the R6 blocker presented three options (pre-capture, source from `coffees/`, defer to follow-up) without investigating any of them first. User corrected with "Do it here and go ahead and plan" — the right behavior was to check if 246/247/249's profiles were in `coffees/` before asking the user to pick an option. **Consequence**: Pushed a decision onto the user that I could have resolved with a 30-second grep, delaying the plan phase by one interactive round-trip.

**Problem**: Initial plan kept Task 8 as a single atomic 780-line port of `calculateShotMetrics`, with a Veto Surface entry that said "if the overnight agent runs out of turn budget on Task 8, we escalate to a follow-up rather than pre-splitting speculatively" — but "follow-up" had no backlog number, no trigger, no checkpoint format, and no escalation target. **Consequence**: Critical review correctly flagged this as a toothless escape hatch; required restructuring Task 8 into 8a (helpers + unit tests) + 8b (main algorithm) post-review, which was the cleaner split available from the start.

**Problem**: Initial plan relied on grep-count verification for Task 8 (`grep -nE '^[[:space:]]*round\(' → no matches` + `inspect.signature`). A stubbed `return []` or `raise NotImplementedError` would have passed both checks. **Consequence**: Critical review surfaced that complex tasks had weaker verification than simple ones (Task 3's 5-assert unit test vs. Task 8's signature-only gate) — an inversion relative to task complexity. Required adding Task 8a unit tests with synthetic scale-lost propagation coverage.

**Problem**: Initial plan's PER_FIELD_TOL inline-comment verification (`grep -B 1 '".*":.*[0-9]' ... | grep -c '^#'`) was pure syntactic compliance — any `#`-prefixed line above each entry satisfied it, regardless of whether the comment contained a real JS citation or rationale. **Consequence**: Created a bug-hiding incentive surface where overnight, faced with (a) re-read 780 JS lines to fix a transcription bug vs. (b) add a one-line PER_FIELD_TOL entry with a plausible-looking comment, would be steered toward (b). Required tightening post-review to "root-cause note in comment, not just JS line" plus Recovery Routing that makes allowlist entries the LAST resort.

**Problem**: Initial Task 11 punted profile-fetch plumbing to "the internal equivalent of `manage_profile(action='get', profile_id=shot_data.profile_id)`" and "or the equivalent internal ws_client call" — two non-equivalent paths with no named symbol. **Consequence**: Critical review caught this; required a 30-second grep to find `await ws_client.load_profile(profile_id)` at `server.py:178` as the specific internal call. The information was available at plan-write time but I didn't look it up.

**Problem**: Initial Task 5 instructed writing a comment citing `web/src/pages/ShotAnalyzer/index.jsx:111-112 @ v1.8.0` for HARNESS_SETTINGS provenance, but Task 4 only vendored `AnalyzerService.js` and `parseBinaryShot.js` — NOT `index.jsx`. **Consequence**: The citation was unverifiable on a device-free overnight run; an implementer would either fabricate the comment or skip it. Required softening to "authoritative per spec R8 + research" with the citation relaxed to "future-reviewer context" rather than "verify at commit time".

**Problem**: Initial plan had no Recovery Routing section. When Task 10 parity failed, overnight had zero plan-provided signal about whether to retry Task 10, re-examine Task 8 port, Task 7 walker, or Task 6 sidecars — all four plausible dependencies. **Consequence**: Critical review flagged this as through-line across two reviewer angles. Required adding a Recovery Routing section mapping parity/response/regression failures to specific upstream task revisits.

**Problem**: Invoked `/lifecycle plan port-ddsa-phaseendstop-algorithm-into-diagnose` treating first arg as phase and second as feature, but the skill's parse rule says first word = feature name, second = explicit phase override. **Consequence**: The invocation technically meant `feature="plan", phase="port-ddsa-..."`, which is nonsensical. I still executed the protocol correctly by manually overriding the parse, but the invocation was malformed — the correct form would have been `/lifecycle port-ddsa-phaseendstop-algorithm-into-diagnose` (phase auto-detected as "plan" since spec.md exists and plan.md does not).

**Problem**: Attempted to run `/retro` via Skill invocation early in the session when a retro for this same work already existed at `retros/2026-04-21-2055-lifecycle-018-refine.md`. **Consequence**: Caught before writing a duplicate (checked existing retros first), but the initial impulse to invoke `/retro` without first checking for an existing recent retro for the same context would have created noise. The user's "Retro written." status line in the opening message was the signal I should have parsed immediately.

**Problem**: ScheduleWakeup fired after the reviewer agents' task-notification pings had already pulled me back in-session, causing the wakeup to prompt completed work. **Consequence**: Produced a redundant "already completed" reply; no real harm but a minor artifact of scheduling a wakeup-based continuation in a session that also receives task-notification pings for the same events.
