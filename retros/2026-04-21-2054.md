# Session Retro: 2026-04-21 20:54

## Problems

**Problem**: Dispatched 4 research agents in parallel; 3 of them (Codebase, Web, Tradeoffs) hit rate limits and returned empty. Only Requirements completed in the first attempt. **Consequence**: Required a second dispatch pass to re-run the three failed agents, wasting the tool calls burned by the first attempt and extending the session.

**Problem**: Web research agent wrote its findings directly to `research/gaggimate-1-8-0-upgrade/verification-notes.md` — the spike's actual deliverable file — rather than returning findings to the orchestrator. The agent prompt listed that path as context, which the agent interpreted as a write target. **Consequence**: Had to `git checkout` to revert, and the premature write could have contaminated the spike if not caught — the Research agent effectively executed part of the Spec's deliverable before Spec was written.

**Problem**: Original spec had self-reporting acceptance criteria on Req 4 (six adversarial audits with "observable from the spike's source evidence sections" as the only AC), no external predicate. Critical review flagged 9 distinct evasion paths including a soft-fallback verdict state that created a strictly dominant strategy for a rushed author. **Consequence**: Required a full spec rewrite after the critical-review pass; the first-pass orchestrator review missed all 9 issues because it graded on checklist-item structure, not on adversarial resistance.

**Problem**: Didn't reconcile the user's "lightweight progressive-disclosure documentation/skill" answer on capture-trigger with Req 7's "no follow-up ticket spawned" and Non-Requirements' "no upgrade-documentation implementation" when drafting the first spec. **Consequence**: Created a logical contradiction (recommendation "deferred to separate follow-up" + no follow-up can be created) that critical-review caught and forced the Req 8 rewording pass.

**Problem**: Req 4.2's row-count acceptance said "≥ 862" without clearly distinguishing "total across both tags" from "per tag" — wrote "862 per tag" in the collapsed-range fallback, which implied 1724 total. **Consequence**: Caught the inconsistency just before presenting for approval; had to patch the spec with a clarifying edit before the approval surface could be shown.

**Problem**: Over-structured the Bash event-log append — used `printf` with escape sequences into `>>` redirection, which is error-prone for YAML content with colons and special chars. **Consequence**: Had to use `printf` with a heredoc-like format to avoid shell interpretation; simpler Write tool on the events.log would have been cleaner.

**Problem**: Didn't verify at Clarify time that 016's checked-in fixtures were all post-upgrade. Only learned this during Codebase Agent's 016 README read in Research, which then materially reshaped the spec (pre-upgrade fixture availability collapsed to "confirmed unavailable"). **Consequence**: Clarify's confidence assessment rated scope boundedness "High" partially on the assumption that 016 provided reference points; the assumption was technically correct (016 provides methodology) but misleading about the pre-upgrade half of the drift comparison.
