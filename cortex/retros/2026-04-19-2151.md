# Session Retro: 2026-04-19 21:51

## Problems

**Problem**: First-pass plan included Task 8d that temporarily mutated `transformers/shot.py` with `+ 0.01` drift and relied on prose-only revert, contradicting Task 8's own "Files: none (verification-only; no code changes)" header and the plan's "No modification of parser, transformer..." scope boundary. **Consequence**: Critical review surfaced the self-contradiction and the operational fragility (sandbox interruption between inject/revert, auto-commit collision, formatter hooks) — required removing the task entirely on rewrite; the drift-detection property is already hermetically proved by Task 2 case (ii)+(viii).

**Problem**: First-pass plan had Task 2 case (vi) say "list length mismatch — the test pins whichever the walker implements," explicitly deferring contract definition to implementation time. **Consequence**: Critical review flagged this as a test-that-rewrites-itself pattern; two conforming walker implementations could emit different `kind`s for the same drift, invalidating Task 8d's field-path assertion. Had to pin list-length as a single `"length"`-kind short-circuit.

**Problem**: First-pass plan left `max_mismatches=10` recursion cutoff and traversal order unspecified. **Consequence**: Reviewer demonstrated Task 2 case (ix) "assert exactly 10 returned" is unwritable as a stable test without pinning ordering; two valid walker implementations produce different mismatch sets. Had to pin sorted-key traversal + immediate-return cutoff on rewrite.

**Problem**: First-pass plan did not address positional list drift in the walker — an insertion at `samples[12]` in a 200-sample phase would cascade into 188 value-mismatches, with the `max_mismatches=10` cap trimming all output to `samples[12..21]`. **Consequence**: The regression harness's primary use case (transformer drift on real shots) would produce maximally misleading output; required adding the `len(expected) != len(actual) → single "length" Mismatch, no per-element walk` rule.

**Problem**: First-pass plan Task 3 listed `api/http.py` as "optionally" modifiable and punted the inline-vs-refactor decision to the implementer via Veto Surface ("lets the implementer choose"). **Consequence**: Reviewer identified this as silently breaking the spec's "purely additive" framing, creating two materially different PR review surfaces (additive +20 LOC vs touched-production-file refactor with new API surface) with no acceptance criteria for the refactor path. Had to pin inline-aiohttp and explicitly enumerate the 5 behaviors to duplicate.

**Problem**: First-pass plan Task 5 assumed diversity floor (2+ profiles, 2+ origins, ≥3 count) was always meetable from device history + no hardware sessions allowed, with escape-hatch language covering only archetype (c). **Consequence**: Reviewer demonstrated archetypes (a), (b), AND the origin-diversity constraint were equally vulnerable given session memory (bloom-slide-exclusive dialing across 4 recent coffees) + CLAUDE.md's documented 1.8.0 sidecar-purge-on-capacity-eviction trap. Had to split into Task 5a (feasibility pre-flight with explicit 3-option escalation: relax / hardware-session / defer) + Task 5b (capture).

**Problem**: First-pass walker spec left the `Mismatch` dataclass schema unpinned for key-absent cases — no rule for what goes in `expected` when `kind="extra_key"` or `actual` when `kind="missing_key"`. **Consequence**: Two conforming implementations could use different sentinels (`None`, key-name string, missing-marker object), silently invalidating future message-text assertions. Had to add module-level `_MISSING = object()` sentinel and pin the schema on rewrite.

**Problem**: First-pass plan did not state transformer-output invariants (NaN-free, tuple-free, sign-zero-free) that the walker's `==` semantics implicitly depend on. **Consequence**: Reviewer demonstrated `nan != nan` would fail byte-stability while walker passes, and `-0.0 == 0.0` under `==` but their `repr` differs — silent contract disagreements between Task 1 and Task 8c. Had to document the invariants in Task 1 Context and surface them in the README's "Walker contract surprises" section.
