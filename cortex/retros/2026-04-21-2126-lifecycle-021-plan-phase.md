# Session Retro: 2026-04-21 21:26

## Problems

**Problem**: First `/lifecycle` invocation passed `plan 021` as args, which the skill parsed as feature="plan", phase="021" — neither valid. **Consequence**: One full skill launch wasted; had to re-invoke with the full slug `post-upgrade-drift-investigation-mixed-era-retention-ordering-ble-precision` and let artifact-based phase detection pick plan automatically.

**Problem**: `/refine` wrote events.log entries in YAML-block format, but the `/lifecycle` skill reference specifies NDJSON (one JSON object per line). No contract exists between the two skills on format. **Consequence**: Had to choose whether to match the existing YAML format (did) or switch mid-file to NDJSON; state-detection logic would misread either mix; format divergence persists as a latent skill-integration issue.

**Problem**: `/refine`'s `lifecycle_start` event omitted the `criticality` and `tier` fields that `/lifecycle`'s state-detection requires. Default fallback is `medium`/`simple`, but the backlog frontmatter showed `high`/`complex` — user-approved during refine. **Consequence**: Had to backfill two `*_override` events at session start to reconcile events.log with the backlog's authoritative state; future resumes will now detect correctly, but the gap is in `/refine`, not fixable from `/lifecycle` invocation.

**Problem**: `/lifecycle` skill's backlog write-back instruction hardcodes `lifecycle_phase=research` in the example `update-item` command, which is wrong when resuming at plan/implement/review phase. **Consequence**: Had to adapt the command to `lifecycle_phase=plan`; a literal reader of the skill would have regressed the backlog's phase field.

**Problem**: First plan draft had systemic self-sealing verification across Tasks 3, 6, 7, 8, 9a, 9b, 11 — every source-audit grep anchor could be passed by writing the expected string with no actual audit. Orchestrator review cycle 1 passed P7 on the "artifact IS the deliverable" rationale, which conflated shape-equals-content cases (Task 10 diff block) with shape-is-proxy-for-audit cases. **Consequence**: Orchestrator review missed the systemic issue; critical-review's Angle 1 surfaced it and forced 7+ verification-field rewrites in the apply-feedback pass.

**Problem**: First plan draft had zero tasks performing the spec's Edge Case 7 re-fetch anti-hallucination mitigation. Spec line 70 explicitly named the mitigation as "independent `WebFetch` re-fetch of every cited blob URL." **Consequence**: Plan degraded a spec invariant; had to add a new Task 14 post-critical-review, renumbering original Task 14 → 15 and updating dependency edges in Tasks 14 and 15.

**Problem**: First plan draft's private-repo read-only invariant (Tasks 1 + original 14) used global `git status --porcelain` + `git rev-parse HEAD` diff, which conflates "spike did not write" with "nothing in the repo changed during the window." CLAUDE.md's auto-commit policy fires on `/feedback` / `/new-coffee` / `manage_shot_notes` — any concurrent user workflow would fail the diff even when the spike is clean. **Consequence**: Semantic mismatch between Req 5 ("spike reads but does not write") and the verification's time-scoped diff; had to add a recovery-path classifier to Task 15 post-critical-review.

**Problem**: Orchestrator review cycle 1 flagged P1 (Task 9 out of sizing bounds) but not P1 for Tasks 3, 5, 6, 7, 8 — all also oversized at ~20–45 min each. Flagged only Task 9 for split, documented the others as "inherent-to-deep-audit" in Veto Surface rather than splitting them. **Consequence**: Sizing-guideline compliance is partial; the plan's honest envelope for the source-audit tasks exceeds the 5–15 min guideline by 2–3×, which the overnight runner's turn-budget logic may not accommodate.

**Problem**: `orchestrator_dispatch_fix` for Task 9 split used `mode: same_context` even though the skill documents that mode for "interactive rework that requires user input" — the fix did not require user input. Rationalized as "narrow structural edit, subagent dispatch would amortize 17KB plan reload onto one-task edit." **Consequence**: Procedural soft-divergence from skill guidance; audit trail captures the rationale but the skill's mode taxonomy is not quite right for this case.

**Problem**: Skimmed research.md rather than reading it in full during plan design. Relied on the spec's Technical Constraints and Edge Cases sections to recover research findings. **Consequence**: Structural decisions were spec-driven; research's methodology discussion (e.g., on how to decompose the byte enumeration) was underused, and the Edge Case 7 re-fetch gap should have been visible from research-cross-reference at draft time rather than post-critical-review.

**Problem**: First plan draft listed scratch-dir `.gitignore` handling as an unresolved Veto Surface item ("user may prefer the former for cleanliness") rather than deciding at draft time. **Consequence**: The ambiguity surfaced again during critical-review synthesis (Concerns section, Concern 2), requiring a decision in the apply-feedback pass — an extra round-trip for a decision that could have been made upfront.

**Problem**: Opus synthesis prompt for critical-review inlined all four reviewer findings verbatim rather than letting the synthesis agent read them from tool results. The inlining meant the synthesis agent received a potentially framed / reformatted version of the findings. **Consequence**: Minor anchoring risk — the synthesis agent's "through-lines" and "tensions" sections may reflect my inline summary's structure rather than the raw agent outputs.
