# Session Retro: 2026-04-21 20:55

## Problems

**Problem**: Clarify phase committed to option (a) "browser-side export script" for reference-JS capture based on ticket text, without verifying that `calculateShotMetrics` is actually reachable from DevTools in the production device-served Vite bundle. **Consequence**: Research-phase adversarial agent surfaced that production bundles minify/scope-wrap ES exports so option (a) requires standing up a local dev build + device proxy (1–2 hours + per-fixture manual replay) — materially different from ticket framing. User had to re-choose the capture strategy in the Research Exit Gate after already answering Clarify Q1, introducing rework.

**Problem**: Research agents (Codebase + Web) confirmed `calculateShotMetrics` exists but did not verify its input contract — it expects pre-parsed samples, not `.slog` bytes. **Consequence**: Spec R8 was written claiming a single vendored `AnalyzerService.js` + zero-dep `capture.js` would work; critical-review Node-harness angle discovered `parseBinaryShot.js` is a required second file. Spec had to be amended post-critical-review to add the second vendored JS file and update the diff step in the runbook.

**Problem**: Codebase research agent cited `.claude/skills/diagnose/SKILL.md:112-126` as "Phase Comparison" and I copied that citation into spec R13 and the Changes-to-Existing-Behavior section without verifying. **Consequence**: The actual `### Phase Comparison` heading is at line ~231 inside the Response Format template; lines 112-126 are an instructive "2b. COMPARE Intended vs Actual" section with different semantics. Critical-review caught the error; spec required rewriting R13 + the Changes entry.

**Problem**: Spec R5 introduced three `unavailable_reason` Literal values (`profile_evicted`, `firmware_version_mismatch`, `insufficient_samples`) without verifying each had a producer. **Consequence**: Two of three were dead enum values — `firmware_version_mismatch` had no detection mechanism (mDNS explicitly rejected in Non-Requirements), and `insufficient_samples` had no threshold or detection site. Critical-review caught both; spec had to narrow the Literal to one value (`profile_unavailable`) and the `profile_evicted` name itself misnamed 4 distinct failure modes (device offline, UUID missing, renamed, WebSocket fail).

**Problem**: Spec R12 referenced an undefined `raw_shot_id` variable in the `analyzer_url` construction formula. **Consequence**: Three candidate variables exist in `analyze_shot` (the `shot_id` parameter, `normalized_id = shot_id.zfill(6)`, and `TransformedShot.shot_id`) — spec didn't pick one, leaving implementer to guess. Critical-review surfaced; spec had to clarify that the caller-input `shot_id` is the URL source.

**Problem**: Attempted to present 5 Open Question decisions via AskUserQuestion in the Research Exit Gate. **Consequence**: Hit `InputValidationError` (max 4 questions per AskUserQuestion call); had to drop Q5 (exit-reason vocabulary `time` vs `duration`) and carry it as a research recommendation to be codified by Spec.

**Problem**: Initial dispositioning summary after critical-review used a numbered list with descriptive prose ("APPLY (14): 1. Fix R13..."). **Consequence**: User said "Try again" — format didn't match critical-review's required compact summary spec which mandates direction verbs (strengthened/narrowed/clarified/added/removed/inverted) per bullet. Had to redo the summary in the correct format.

**Problem**: Spec was written without checking whether overnight execution could proceed offline. **Consequence**: User had to ask "Can this work be done without being able to connect to the coffee machine?" as a post-hoc check — revealing that R6 (profile JSON capture) is the one device-dependent step the spec didn't flag as an execution prerequisite. The spec treats fixture capture as part of the implementation work but doesn't distinguish "needs device once" from "runs offline."

**Problem**: Spec R11 originally did not require inline-citation comments for per-field tolerance entries. **Consequence**: Critical-review's version-drift angle flagged this as a false-confidence vector — allowlist growth could mask bugs over firmware releases. Had to amend R11 post-critical-review to require inline `#`-prefixed JS-source-line comments per entry.

**Problem**: R14 runbook originally ordered `ANALYZER_JS_VERSION` update as the last step. **Consequence**: Critical-review pointed out this ordering makes "new Python + new sidecars + old constant" indistinguishable from correct state if the final step is skipped or done out of order. Had to invert the ordering so the constant is updated before JS vendoring and sidecar regeneration.

**Problem**: Areas assignment was uncertain because the canonical list in /refine (overnight-runner, backlog, skills, lifecycle, hooks, report, tests, docs) doesn't match this repo's convention. **Consequence**: Had to grep existing backlog items to discover the repo uses custom areas (`mcp`, `tests`, `docs`), then pick `[mcp, tests]`. /refine's canonical-list guidance assumed a different project structure than this repo uses.
