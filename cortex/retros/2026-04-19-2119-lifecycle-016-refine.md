# Session Retro: 2026-04-19 21:19

## Problems

**Problem**: Asked a 3-question AskUserQuestion (golden surface / fixture priority / JS sidecars) before the user had plain-English context for what we were building. **Consequence**: User rejected the tool call and asked "Can you explain what we are adding here?" — had to step back and explain the ticket in non-jargon terms before re-asking.

**Problem**: Asked whether 016 should capture DDSA JS reference sidecars without first checking 018's ticket body. **Consequence**: User answered "Sure grab it" based on my framing, but 018 owns the JS-capture mechanism entirely (with its own 3-option "Implementation prerequisite" section) and none of those options exist yet — "grab it" would have meant building a mini-018 inside 016. Had to self-correct and re-ask with accurate scope.

**Problem**: Wrote the initial spec carrying forward the ticket's `1e-6` float tolerance without first-principles math check. **Consequence**: Critical review caught that the transformer rounds RMSE to 2 decimals, so min representable delta is 0.01 = 10,000× the tolerance. The tolerance was a phantom guard — either unnecessary (delta = 0) or insufficient (delta = 0.01 boundary-crossing). Had to rewrite R4 to exact equality and document the pre-rounding invariant as load-bearing.

**Problem**: Estimated the deep-equality walker as "~30 LOC" in both research and spec without enumerating the contract requirements. **Consequence**: Critical review showed correct implementation (list-index paths, `isinstance(bool)` before numeric, continue-through-N-mismatches, extra/missing key reporting, custom format) is 50-80 LOC + its own unit test file. Carried wrong sizing into the effort estimate downstream.

**Problem**: Included Edge Case 59 (non-terminating binary float repr like 0.1 is safe) in the Edge Cases section. **Consequence**: Critical review flagged it as reassurance, not an edge case — "has no behavior to implement, it's a note to a reviewer." Implied scope the implementer didn't need to handle. Moved to Technical Constraints.

**Problem**: Escalated the ticket body's one-line "flag CI as follow-up" into a formal grep-verifiable requirement R9 with cross-ticket coupling to `backlog/013-*.md`. **Consequence**: Critical review pointed out this creates write-coupling to an artifact whose reshape history (`f2b9b24` "9→6 children") proves it's unstable; the breadcrumb would rot. Weakened to PR-description note.

**Problem**: Wrote R6 refresh CLI as device-fetch-only without thinking through each downstream caller's workflow. **Consequence**: 015 is a transformer-only change that doesn't need to touch `.slog` bytes, but R6 would force pointless device I/O and a data-integrity risk (committed bytes vs device bytes drift). Critical review caught it; had to split R6 into default mode (goldens-only from existing `.slog`) and `--fetch` mode (initial capture).

**Problem**: Asserted "No new dev dependencies" as a Non-Requirement without arguing it in spec — carried the default forward from research. **Consequence**: Critical review flagged this as cargo-cult: `mcp/pyproject.toml` already has 3 dev deps, so "zero" was a false framing. Deferred to user, who asked me to recommend — the recommendation needed to be reasoned from scratch rather than inherited from the research default.

**Problem**: Did not know firmware wipes shot history on upgrade; research and spec both assumed "pre-upgrade shots may still exist under capacity-based retention." **Consequence**: Spent effort designing eviction-insurance mechanisms (Open Decision for pre-upgrade shot preservation). User's answer — "There are no surviving pre-upgrade shots on the device itself. It seems to get wiped on upgrade" — collapsed the whole branch. This was experiential device-behavior knowledge the user had but wasn't in any readable source; research agents could not have reached it.

**Problem**: Declared Open Decisions "None deferred" in the first spec draft while R8 did in fact defer fixture identity to implementation time. **Consequence**: Critical review flagged the misclassification. Had to reclassify fixture identity as a genuine Open Decision.
