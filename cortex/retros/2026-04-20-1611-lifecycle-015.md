# Session Retro: 2026-04-20 16:11

## Problems

**Problem**: Chose the lifecycle slug from the backlog filename (`surface-weight-flow-in-transformer`) instead of the canonical title-derived slug (`surface-weight-flow-g-s-in-transformedsample-flowsummary`) and had to rename the lifecycle directory, rewrite `index.md`, update the `events.log` feature field, and run a second `update-item` for `lifecycle_slug` after checking existing lifecycles showed the title-derived convention. **Consequence**: Wasted several tool calls and created a transient inconsistent state in the events.log before the correction landed.

**Problem**: Accepted Agent 1 (Codebase reviewer)'s claim that the fixture walker lived at `mcp/src/gaggimate_mcp/tools/shot_fixture_walker.py` without verifying, then had to glob-find it at the correct path `mcp/tests/shot_fixture_walker.py` only after the file-listing check failed. **Consequence**: The initial spec version carried a wrong file path in Technical Constraints that would have misdirected an implementer had it shipped.

**Problem**: Wrote the spec's canonical worked example (fixture 247 tare spike) based on research-agent summaries rather than inspecting raw parsed sample data; the example claimed `pf=0.0 and v=0` at t=0.75s when actual raw data has `v=6.9` (weight mirrors the tare event). **Consequence**: R4's cup-weight cross-validation was inadequate as specified — critical review disproved the worked example, forcing a full Unified Hygiene Rule redesign (adding `pf > 0` cross-validation across R2/R3/R4) and a second user interview.

**Problem**: Recommended shot 170 as a "clean reference" fourth fixture in the Spec interview based on MEMORY.md's "DIALED IN: Shot 170" line, treating MEMORY.md as current device state rather than historical extraction record — despite shot 170's eviction being documented in both `mcp/tests/fixtures/shots/README.md` and CLAUDE.md's 1.8.0 retention-trap note. **Consequence**: User picked the recommendation; critical review later caught the eviction; required a second user interview to drop R6 entirely and rewrite the positive-signal coverage strategy in Non-Requirements.

**Problem**: Rationalized skipping the fresh-subagent fix-dispatch in orchestrator-review cycle 1 by labeling the AC tightenings as "mechanical" and logged the fix with `mode: same_context` despite the protocol prescribing `same_context` only for "interactive rework that requires user input". **Consequence**: The protocol's fresh-context-reduces-anchoring property was bypassed; critical-review subsequently found issues (negative assertions, typos, line-number fragility, unresolved placeholders) that a fresh-agent rewrite would have been likelier to surface earlier.

**Problem**: Ran `replace_all` on `000170` → `170` across the spec without first checking where the string appeared, which corrupted the Technical Constraints example `shot 170 → 000170` into the malformed `shot 170 → 170`. **Consequence**: A zero-padding typo shipped into the spec; critical-review reviewer 4 had to catch it and required another rewrite pass to fix it correctly.

**Problem**: Anchored spec requirements and Technical Constraints to specific line numbers in `transformers/shot.py` (`~line 63`, `line 179`, `lines 227–228`, `lines 316–321`, etc.) even though every one of them would silently drift after the first implementation edit and no AC enforced them. **Consequence**: Critical-review flagged the brittleness; required a full spec rewrite to strip line-number anchors and replace with function/class names.

**Problem**: The two-cycle orchestrator-review pass failed to catch several issues that critical-review then surfaced — factually-wrong worked example, evicted fixture, line-number brittleness, unresolved placeholders like `{fixture-readme-path}` and `<pre-regen-commit>`, `pytest -k 170` substring-match fragility, `'banker'` grep-literal synonym gap. **Consequence**: Post-approval critical-review drove a much larger rewrite than the two earlier orchestrator cycles did; the orchestrator checklist as applied didn't function as a real quality filter for this spec.
